{
  "name": "graphql-mask",
  "version": "0.0.0-semantic-release",
  "description": "GraphQL Mask lets you subtract everything outside a schema from a query",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "coverage": "jest --coverage",
    "coverage:watch": "jest --coverage --watch",
    "prepublishOnly": "yarn build",
    "build": "rimraf es5 && babel src -d es5"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "graphql": "^14.1.1",
    "jest-cli": "^25.2.7",
    "rimraf": "^3.0.0",
    "uglify-js": "^3.1.1"
  },
  "author": "Bryan Goldstein",
  "homepage": "",
  "license": "MIT",
  "repository": "git@github.com:brysgo/graphql-mask.git",
  "peerDependencies": {
    "graphql": "*"
  }
}
